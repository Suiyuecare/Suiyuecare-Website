# SEO 第二輪改善 1、2、3（2026-09-12）

本次由正式版本 `1006292a426437dd96886a3d85b7f69dec6d73e9` 建立隔離副本，原工作目錄的未提交內容保留。因原 Git 物件的 mmap 讀取卡住，另從 GitHub 取得同一版本的乾淨物件，不修改或清理原 Git 資料。

## 1. 搜尋成效與頁面歸因

- 後台 `/admin/traffic` 的 SEO 分頁新增 Google Search Console CSV 匯入。接受解壓後 Queries／Pages 的 UTF-8 英文或繁中 CSV，查詢與網頁分開分析。
- 提供匯入列曝光、點擊、重算 CTR、曝光加權排名，以及可調的曝光／CTR 門檻。改善排序是明示的篩選規則，不是 Google 評分或排名保證。
- 未知值保留「—」；不把匯入列加總當成整站完整成效。期間由管理者依匯出範圍填寫；不任意串接不同期間或查詢／網頁維度。
- 資料僅在目前頁面的記憶體中分析，可清除與依權限匯出；不新增 Google OAuth、API、資料庫、瀏覽器持久儲存或資料上傳。使用既有 `can_view_analytics`／`can_export_analytics` 權限。
- 修正正式路徑上的事件、表單來源誤記成 `#home`，將 page view 與事件統一成去除查詢字串的路由鍵。離頁 engagement 明確記錄原頁。
- 修正後台 toast 把不可信檔名再當 HTML 插入的 XSS 鏈；使用 DOM/textContent，登入及權限語意不變。
- 尚未取得真實 Search Console 匯出檔，所以未產生網站關鍵字排名或機會結論。瀏覽器報表驗證使用明確標記的合成 fixture。

操作：Search Console「成效／搜尋結果」選擇期間、開啟四項指標、匯出 CSV 並解壓；在網站後台 SEO 分頁先匯入 Queries.csv，再匯入 Pages.csv，可切換維度檢視。Google 匯出資料可能受列數限制，請保留原報表期間與篩選條件。

## 2. 確認重複內容的單一入口

- `article43` 與來源別名 `day-care-transition` 永久轉至 `article7`，每個入口一次到位。Vercel 的 `permanent: true` 產生永久轉址。
- 保留 `ARTICLE_SOURCE_SLUGS` 的原編號，article43 不會重新分配；CMS 資料未刪除。要修改保留文章，請編輯 `daycare-first-week`（article7）；article43 的公開入口已整併。
- 公開文章清單、搜尋清單、相關推薦、sitemap 與靜態正文排除重複入口，保留 159 篇有效文章。原始 CMS identity／public number contract 不變。
- 保留 article7 的署名、日期與正文，不冒稱新審閱或更新。前端舊 hash 導航也導向保留入口。
- 依據：兩篇主要正文、清單及表格空白正規化後相同（799 字元）。站內資料全期／近 90 日：article7 12 次瀏覽、9 次 organic；article43 4 次、2 次。近 30 日則為 2／1 與 4／2，樣本很少且方向不同。本次選擇穩定的既有入口，並不宣稱已確認 Google 排名勝出或外部連結優勢。

## 3. 日照指南與相關閱讀

- 新增 `/guides/day-care`，依申請費用、參觀準備、適應參與、接送交接串連 article12、29、7、138、134，費用連回現行服務說明，不複製補助額度。
- 健康3.0 的「認識日照」直接進指南。日照服務頁增加 29／7／134；居服增加 12／133／137。
- 文章推薦優先人工選文，再依服務、分類與標籤匹配；排除自身、重複、不安全網址、明確未發布及未來文章。
- 指南具有完整初始 HTML、獨立標題、canonical、麵包屑結構資料及 sitemap 入口。
- 七筆公開文章卡片的輕量快照讓服務與指南的連結不會在完整文章程式延遲載入時被縮減。CMS 更新時保留原表單 DOM 與使用者輸入。
- 舊 hash 指南導航先換成正式路徑，頁內段落連結不再返回首頁。日照據點錨點會等費用內容完成，先計算前方區塊實際高度，再對齊一次；手動捲動後不會被拉回。

## 驗證與發布狀態

- 所有變更在隔離副本完成。未 push、未部署本輪變更、未送出正式業務表單。
- `pnpm verify:all` 最終完整檢查通過（exit 0，包含四項 SEO 新功能驗證與後台 toast 安全回歸）；記錄：`/tmp/suiyue-seo-next-verify-all.log`。
- 瀏覽器驗證通過：390px／1440px 指南、五篇文章實點、舊文章入口、指南與據點錨點、單一表單及預填、CMS 載入後連結完整度／輸入保留、analytics 攔截歸因。未見水平溢出、可見破圖或 console errors。
- 後台 CSV 使用真實 HTML 與模組的獨立 fixture 驗證，沒有繞過正式登入。
- 最終檢查結果與提交版本以本次完成訊息為準。

參考：[Google Search Console 成效報表](https://support.google.com/webmasters/answer/7576553?hl=zh-Hant)、[Google 重複網址整合指引](https://developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls)。
